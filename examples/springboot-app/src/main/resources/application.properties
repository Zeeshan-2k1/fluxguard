# FluxGuard Spring Boot benchmark example — Redis URI and server port.
server.port=8080
spring.application.name=fluxguard-benchmark-java

fluxguard.redis.uri=${FLUXGUARD_REDIS_URI:redis://localhost:6379}
